{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ events|length }} Events</h1>

    <ul>
    {% for event in events %}
        <li>
            <strong>{{ event.name }}</strong> in <em>{{ event.location }}</em>:
            {{ event.price }} €
        </li>
    {%  endfor %}
    </ul>

{% endblock body %}